node_modules/
.next/
/.claude
.codex
.env
.env.*
*.log
.DS_Store
.vscode/
.idea/
*.swp
*.swo
*~
Thumbs.db
coverage/
.nyc_output/

# Build output
dist/
.playwright-mcp/

# npm pack artifacts (local dev-bridge tarballs)
*.tgz

# Package manager files (keep package-lock.json for npm packages)
# package-lock.json  <- Keep this for reproducible builds
yarn.lock
pnpm-lock.yaml

# Local MCP config
.mcp.json

# Manual test scripts
tests/integrations/mcp/test-mcp-server.mts

# Local playground (scratch dev / runtime probes)
playground/
# Per-run scratch dir for load-resources-import-compat.test.ts. Windows can
# hold module handles past afterAll, so removal is best-effort and a leftover
# must not show up as untracked noise.
tests/factory/__tmp_import_compat__/
