# Wiki Log

Append-only. Newest at bottom. Format: `YYYY-MM-DD — <page|all> — <change>`.

---

- 2026-04-21 — all — initial scaffold. Seeded pages from CLAUDE.md + AGENTS.md for arc v2.10.3.
- 2026-04-24 — core — v2.11.0 mixin split documented (BaseCrudController + 4 mixins, declaration-merged generic threading, systemManaged-from-required strip, caller-config mutation fix, schema-error arcLog warn).
- 2026-04-24 — gotchas — added #19-24 for v2.11 (mixin composition, systemManaged required strip, types subpath cleanup, defineResource hygiene, schema-error warn, resourceToTools split).
- 2026-04-24 — core — documented `createActionRouter` + `routerShared` canonical preHandler order (preAuth → arc → auth → permission → pluginMw → routeGuards) and the three co-landed v2.11 action-router parity fixes: dynamic permission at canonical slot, IControllerResponse pipeline preservation, omitted-public auth handling.
- 2026-04-24 — gotchas — added #25 (action router permission ordering parity + pipeline error preservation + omitted-public handling via `buildActionPermissionMw` / widened `buildAuthMiddlewareForPermissions`).
- 2026-04-24 — testing — full rewrite of `src/testing/` public surface for v2.11. HttpTestHarness is primary; TestHarness (Mongoose-bound, 0 consumers) deleted along with authHelpers + dbHelpers + old testFactory (1,535 LOC dead code removed). New: TestAuthSession (unified auth), TestFixtures (DB-agnostic seeding), expectArc (fluent envelope matchers), createTestApp (turnkey factory). tests/setup.ts rewritten on top of the public API. Wiki synced to match.
- 2026-04-24 — integrations — websocket.ts (682 LOC monolith with 7 concerns + 2 duplicated fakeReply shims) split into `src/integrations/websocket/` with 7 focused submodules. Public entry (`src/integrations/websocket.ts`) is now a barrel. Auth handshake + re-auth loop share a single `authenticateWebSocket` boundary (was: two drifting shims). `any` cast count: 7 → 1. Gotcha #33 added.
- 2026-04-24 — factory — `resources` option accepts a factory function (sync or async) that runs AFTER `bootstrap[]` but BEFORE route wiring. Canonical answer to "my repository lives in an async-booted engine" — replaces per-resource lazy-bridge adapter boilerplate (~185 LOC typical). Contract: explicit `resources` (including function returning `[]`) wins over `resourceDir` auto-discovery. `plugins → bootstrap → resources-factory → resources-registered → afterResources` lifecycle lock. Boot order updated in CLAUDE.md + `docs/production-ops/factory.mdx`. 9 scenario tests in `tests/factory/resources-factory.test.ts`.
- 2026-04-24 — docs — mixin vs custom-routes decision table added to `docs/framework-extension/handler-patterns.mdx` (Pattern 3). Covers the silent hazard where a `routes: [{ path: '/by-slug/:slug' }]` entry shadows a `SlugMixin`-generated URL, leaving the `repo.getBySlug` method dead but reachable via other means. Zero code change — doc-only.
- 2026-04-24 — testing — `src/testing/betterAuth.ts` re-shipped after the initial 2.11 rewrite deleted it. Field report: 24 downstream test files used `createBetterAuthTestHelpers` / `setupBetterAuthOrg` / `safeParseBody`; deleting on "zero arc-internal consumers" was the wrong metric. New `setupBetterAuthTestApp` orchestrator composes over `TestAuthProvider` — one call produces an app + orgId + users + ready-to-use `TestAuthProvider`. 21 unit tests. Docs: `docs/testing/auth-helpers.mdx` rewritten from deprecated redirect into full API reference. DX gotcha: don't delete public utilities based on internal adoption only — survey downstream before removing.
