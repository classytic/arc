# Wiki Log

Append-only record of **wiki page** edits. Newest at bottom. Format: `YYYY-MM-DD — <page|all> — <wiki change>`.

This log tracks what's been documented in the wiki and why; release notes live in [`/changelog/v2.md`](../changelog/v2.md).

---

- 2026-04-21 — all — initial scaffold from CLAUDE.md + AGENTS.md (arc 2.10.3 baseline).
- 2026-04-24 — core — mixin-split surface documented (`BaseCrudController` + 4 mixins).
- 2026-04-24 — gotchas — added #19–24 covering 2.11 surface (mixin composition, `systemManaged` required-strip, `/types` cleanup, `defineResource` hygiene, schema-error warn, MCP file split).
- 2026-04-24 — core, gotchas — `createActionRouter` canonical preHandler order documented; gotcha #25 added.
- 2026-04-24 — testing — rewritten against the new `@classytic/arc/testing` surface (`createTestApp`, `TestAuthProvider`, `TestFixtures`, `expectArc`, `createHttpTestHarness`).
- 2026-04-24 — gotchas — added #33 for the websocket module split.
- 2026-04-24 — factory — documented `resources` accepting a sync/async factory function (between `bootstrap[]` and route wiring).
- 2026-04-24 — testing, removed — `createBetterAuthTestHelpers` / `setupBetterAuthOrg` / `safeParseBody` re-shipped; wiki sync.
- 2026-04-25 — adapters, gotchas — `fieldRules.nullable` documented; `default: null` widening on built-in mongoose fallback. Gotcha #26 (alongside `RouteSchemaOptions extends SchemaBuilderOptions`).
- 2026-04-25 — adapters — `RouteSchemaOptions extends SchemaBuilderOptions` (peerDep `>=0.2.0`); `ArcFieldRule extends FieldRule`. Documents the no-cast `schemaGenerator: buildCrudSchemasFromModel` wiring.
- 2026-04-25 — index, factory, events, plugins, removed — released wiki of historical changelog files (`changelog-v2.9/v2.10/v2.11.md` deleted; release notes belong under `/changelog/`). Cross-links updated to point at concept pages or the canonical changelog. Index "History" section collapsed to "API lifecycle" → `removed.md`.
- 2026-04-25 — factory — `loadResources({ context })` factory-export pattern documented; `silent` flag removal recorded with the arcLog fallback semantics (warn-by-default, suppressible via `ARC_SUPPRESS_WARNINGS=1` or per-call `logger` override).
- 2026-04-29 — events, adapters, peer-deps, identity — phase A–J relocation: events now sourced from `@classytic/primitives/events`; pagination / tenant / errors / schema-generator from `@classytic/repo-core/*`; `MongooseAdapter` and `DrizzleAdapter` schema-gen fallbacks deleted (kit `schemaGenerator` required). Peer-deps + identity bumped to 2.12.0 / primitives required.
- 2026-05-02 — adapters, peer-deps, index, factory, types, identity — arc 2.12 adapter split: every kit-specific adapter moved out of arc into its own kit. Adapter contract (`DataAdapter`, `RepositoryLike`, `AdapterRepositoryInput`, `OpenApiSchemas`, `SchemaMetadata`, `FieldMetadata`, `RelationMetadata`, `AdapterFactory`, `AdapterValidationResult`, `AdapterSchemaContext`, `asRepositoryLike`, `isRepository`) now lives in `@classytic/repo-core/adapter@>=0.3.1`. `createMongooseAdapter` ships from `@classytic/mongokit/adapter@>=3.13.0`; `createDrizzleAdapter` from `@classytic/sqlitekit/adapter@>=0.3.0`; `createPrismaAdapter` from `@classytic/prismakit/adapter@>=0.1.0`. `mergeFieldRuleConstraints` + `applyNullable` now in `@classytic/repo-core/schema`. Arc 2.12 is fully DB-agnostic — zero kit-specific adapters in `src/`. The `src/adapters/` directory was removed entirely; the internal `store-helpers.ts` moved to `src/utils/store-helpers.ts`. The `@classytic/arc/adapters` subpath export was removed from `package.json`. Mongokit, sqlitekit, mongoose, `@prisma/client` dropped from arc's peer-deps table — kits own their driver peers. Custom kits implementing `DataAdapter<TDoc>` plug in identically.
- 2026-05-03 — gotchas, identity — pruned wiki cruft. Deleted gotchas #19/#20/#22/#23/#24/#25/#26 (v2.11 history that's now just "how it works" — release notes belong in `/changelog/v2.md`, not gotchas). Trimmed #21 to the one load-bearing rule ("don't import values from `/types`"). Stripped version tags from #11/#12/#13/#15 and the dual-publish detector. Updated #9 to drop the rotting LOC count. Updated gotchas-file header to state "evergreen only — version-tagged behavior changes belong in changelog." Removed hardcoded `v2.12.0` from `identity.md` shape table (matches AGENTS.md "version numbers rot" stance).
- 2026-05-03 — skills/arc — pruned skill rot. Dropped frontmatter `version: 2.12.0` + `metadata.version` (versions rot — match the package). Dropped `progressive_disclosure.context_limit: 700` (file is 900+ lines; promise was false). Moved the 40-line subpath-imports enumeration out of `SKILL.md` into new `references/api-reference.md` (kept a 7-line common-imports block + a pointer in the entry). Trimmed the Better Auth overlay code block to a 3-line summary that points at `references/auth.md`. Convention-load-bearing content (RequestScope rules, field-write reject default, schemaGenerator-required, MCP fail-closed, multi-tenancy hierarchy, etc.) all preserved.
- 2026-05-03 — skills/arc-code-review — dropped pinned `arc_version` / `mongokit_version` / `sqlitekit_version` / `prismakit_version` / `repo_core_version` from frontmatter `metadata` (the skill versioned itself faster than the workspace; pin rotted). Skill body still cites kit subpaths by name, which is what consumers actually need.
- 2026-05-03 — index, skills/arc, docs/compliance — arc 2.13 enterprise-auth shipment. Three opt-in surfaces, no parallel infra, no forced peer-deps. (1) `@classytic/arc/scim` — full SCIM 2.0 plugin (filter parser, RFC 7644 PatchOp, bearer + verify auth, `/Users` + `/Groups` + discovery endpoints) auto-derived from existing arc resources via `mapping.attributes` (BA-aligned defaults). (2) `@classytic/arc/permissions` gains `requireDPoP` / `requireMandate` / `requireAgentScope` for AP2 / Stripe x402 / MCP authorization flows; `RequestScope.service` extended with optional `mandate` + `dpopJkt` fields (additive — no breaking change); new `Mandate` type + `getMandate` / `getDPoPJkt` accessors in `@classytic/arc/scope`. (3) `@classytic/arc/auth/audit` — `wireBetterAuthAudit({ events, transform, bufferSize })` returns BA-compatible `hooks` + `databaseHooks` config; routes auth events through existing `auditPlugin` so resource + auth audit rows live in one queryable store. New skills/arc/references/{scim,agent-auth,enterprise-auth}.md — surface-level recipes + `playground/enterprise-auth/` runnable smoke (11/11). New docs/compliance/{soc2,hipaa}.md mapping each TSC / Security Rule clause to the arc primitive that satisfies it. Arc still doesn't ship SAML / DPoP verification / mandate JWT parsing / device trust / session storage — see references/enterprise-auth.md "Out-of-box" table for why.
- 2026-05-03 — skills/arc/auth, skills/arc/SKILL.md — surfaced the BA plugin integration matrix that the kit tests actually cover (organization, twoFactor, admin, bearer, apiKey) but the docs glossed over. New "Better Auth plugin integration matrix" + "Multi-plugin recipe" + "Multi-role members" + "registerBetterAuthStubs full options" sections in [references/auth.md](../skills/arc/references/auth.md). Expanded API-Key section to flag the separate `@better-auth/api-key` npm package (NOT under `better-auth/plugins`) and added the `apikey` overlay recipe + the `configId` / `referenceId` field gotcha. Added a Bearer-plugin section for SPA/mobile transport. SKILL.md BA pointer expanded from 3 lines to a working multi-plugin recipe + multi-role + bearer + api-key flow summary so agents can wire a real BA stack from the entry doc without round-tripping the reference. Cross-link from the kit playgrounds preserved.
- 2026-05-07 — engine-backed-resources, index — arc 2.15.0 promotes the factory-export pattern from a buried subsection in `factory.md` to a dedicated recipe page. Worked example covers the resource file (default-export factory), the `app.ts` wiring (`resources: async () =>` + `loadResources({ context })`), the resolution-order contract (path 4 in `loadResources.ts:282-316`), and "when NOT to use a factory export." Linked from `index.md#Core`.
- 2026-05-18 — gotchas, mcp, peer-deps, removed — arc 2.16 sync. Gotcha #6 dropped the deleted `./org/types` example. `mcp.md` adds the per-resource `mcp: false` opt-out (authoritative, evaluated first inside `filterResourcesForMcp`), the AI SDK `buildMcpToolsFromBridges` bridge, and Better Auth `getAuth()` auth mode. `peer-deps.md` floor bumps: `@classytic/primitives >=0.6.0`, `@classytic/repo-core >=0.5.0`, `@classytic/streamline >=2.3.3`; mongokit / sqlitekit examples bumped to current minors. `removed.md` gains a v2.16 section for the `@classytic/arc/org` deletion + root `getUserId` re-export removal with migration targets.
- 2026-05-18 — peer-deps — `@classytic/repo-core` flipped from optional to **required** across the contributor docs (`wiki/peer-deps.md` summary + matrix row, `AGENTS.md §7` row, `CLAUDE.md` peer table). Arc imports the adapter / pagination / tenant / errors contracts from repo-core at runtime; `package.json` already lists it as a required peer (`>=0.5.0`), but the contributor docs still said "No" — drift fixed.
- 2026-05-18 — presets — softDelete row clarified: preset only adds HTTP routes (`GET /deleted`, `POST /:id/restore`) + mixin proxy; tombstone column, read-filter, TTL all live in the kit. New "softDelete — kit responsibilities" subsection contrasts mongokit (`softDeletePlugin({ ttlDays })` → real Mongo TTL index with `partialFilterExpression`) vs sqlitekit (two plugins — `softDeletePlugin` + `ttlPlugin({ field, expireAfterSeconds, mode })` with `scheduled` / `trigger` / `lazy` modes, plus `repo.sweepExpired()` for Workers / Cron Triggers). Linked to the new mongokit + cross-kit integration tests.
- 2026-05-18 — permissions — new "Stamp-from-scope vs validate-from-scope" subsection documenting the canonical answers to the recurring "body-vs-auth permission check" request (xAPI actor IFI, `body.authorId`, `body.customerId`). Two patterns: (1) flat fields → `fieldRules.systemManaged` + `beforeCreate` stamp from scope (server can't be spoofed because the field never reaches the DB from user input); (2) nested vendor envelopes (xAPI's `statement.actor.account.name`) → `beforeCreate` validate, fail with rich per-item context. Explains why arc deliberately does NOT ship a `requireActorMatchesAuth()` `PermissionCheck` helper (category error — auth-gate abstraction vs data validation) and contrasts with `requireOwnership` (read-side, doesn't inspect inbound body). Reinforces "fewer, deeper primitives that compose" stance.
- 2026-05-24 — mcp — arc 2.17.0 arc-ai DX folded in. `mcp.md` documents `createMcpAuthFromBetterAuthApiKey()` (Better Auth API-key → MCP resolver, handles the `referenceId` vs `userId` fallback and the `metadata.organizationId` extraction every host kept reimplementing) under the Auth section, and a new "Tool-name collisions" section pins the `softDelete + actions.restore` auto-namespace contract plus the typed `arc.mcp.tool_name_collision` error for all other collision shapes. Streaming helper (`pipeUIMessageStreamToReply` / `UI_MESSAGE_STREAM_HEADERS`) + `customRoutesOnly: true` shorthand surface in CHANGELOG only — they're host-facing primitives, not concepts; the changelog entry is the canonical reference.
- 2026-06-11 — architecture — internal-debt refactor sweep: `routerShared.ts` → 65-line shim over new `core/middlewares/` (auth, permissions, pipeline, rateLimit, fieldWrite, chain, pluginMw, arcDecorator, registerRoute); `aggregation/validate.ts` → validators + pure `normalize.ts`; defineResource Phase-0 shorthands → `defineResource/normalizeConfig.ts`; `BaseCrudController` 1,225→~960 with pure helpers in `core/crud/{requestPipeline,resourceVerbs}.ts` + options types in `controllerTypes.ts`. Zero behavior/API change. Distributed hardening shipped alongside: bounded failure-context/attempt maps (multi-node leak), BullMQ repeatable-schedule reconciliation at boot, XPENDING recovery scales with batchSize, jittered poll-error backoff. `configure()` now retains construction-time `defaultSort`/`onFieldWriteDenied` across partial rebuilds; `prepublishOnly` runs `test:ci` (perf lane included); default-export exception re-documented as the `fp()` plugin-entry class (was a stale 5-name list).
- 2026-06-27 — encryption (new), index — Application-Layer Encryption plugin shipped at `@classytic/arc/encryption`. New page documents the two modes (full-body JWE via the optional lazy-loaded `jose` peer; field-level AES-256-GCM via `node:crypto`, no dep), the `KeyProvider` contract + `kid` rotation (mirrors `@fastify/secure-session`; Visa two-key-pair model), and opt-in via the new typed `extensions` hatch on `defineResource` (the "React for backend" composition path — threaded onto route `config.arcExtensions` by `createCrudRouter`) OR a plugin-level `routes()` matcher. Load-bearing: all crypto runs in async `preSerialization`, never `onSend`; the onSend race ALSO affects async `preSerialization` on the controller-dispatch path, fixed by `createFastifyHandler` now returning `reply` (was `void`) so Fastify doesn't flush an empty body mid-encrypt. New typed `ResourceExtensions` interface (empty in core; plugins augment their slice via `declare module`) is the additive extension contract — new cross-cutting features ship a subpath + augmentation, no core release per feature.
- 2026-07-05 — permissions, modules, presets, cli — Fable-5 deep-review hardening wave (2.20, pre-publish). SECURITY: `readOnly()` now returns explicit `denyAll` on create/update/delete (pre-2.20 it left writes UNGATED — public-by-omission); new post-preset boot diagnostic `crud-public-by-omission` (warn on ungated writes, info on read-only shapes like `referenceData`); `withApprovalChain` decide no longer trusts `body.approverId` (defaults to authenticated actor, mismatch → 403 `approval.approver_mismatch`, opt-in `canActAsApprover` delegation hook, schema `required` drops approverId); `involvementListScope` now COMPOSES base-permission filters (shallow-merge, `$and` on key collision) instead of dropping them. DX: `ArcModule<TExports>` + throwing `getModuleExports<TExports>(fastify, name)` accessor — module export seam typed end-to-end, `ArcCore.modules` added to the decoration type, tests cast-free. CLI: `arc init` scaffold pins now overlay live truth from arc's own package.json (version + peer floors) — the 2.18.5/0.6.0 drift class is structurally impossible; static fallback synced. RELEASE GATE: `scripts/check-peer-skew.mjs` in `prepublishOnly` fails when a devDep floor < declared peer floor (or a required peer is untested). TYPES: `IPresetController` union now folds to an INTERSECTION (documented multi-preset `implements` finally compiles). Docs sweep: tutorials off `toPlugin()` → `createApp({ resources })` (brownfield note kept, labeled), kit-adapter imports corrected to `@classytic/<kit>/adapter` / `@classytic/repo-core/adapter` (7 files), slug route drift fixed (`/slug/:slug` is canon). Pages updated: permissions.md (public-by-omission section), modules.md (typed seam).
- 2026-07-05 — presets, exports — approval + involvement EXTRACTED before 2.20 publish: `withApprovalChain` (incl. the actor-derived decide identity + `canActAsApprover` fixes above, plus a new `delegateForRoles` helper) now lives in `@classytic/arc-approval`, and `involvementListScope`/`resolveInvolvementScope` (incl. filter composition + header option) in `@classytic/arc-involvement` — both in the `arc-ecosystem` npm-workspaces repo (peers: arc >=2.20). The `./presets/approval` + `./presets/involvement` subpaths, sources, and tests were REMOVED from arc core; they never shipped in any published arc version, so no removed.md entry and no deprecation window. Rationale + execution record: `wiki/record-ecosystem-extraction.md`. Rule going forward: domain packs ship as `@classytic/arc-*` ecosystem packages composed via modules, not as core subpaths.
- 2026-07-06 — mcp, core — 2.20 review follow-ups: MCP tool input-schema REST parity (`list` tool now advertises `cursor` keyset pagination alongside `page`/`limit`; `get` tool advertises `select` projection — both already honored by QueryResolver, only the tool schema lagged); per-route `RouteDefinition.rateLimit` (`{max,timeWindow}` / `false` / inherit) overriding the resource/app default for a single custom endpoint. Actions stay endpoint-shared (no per-action limit — documented). Skill refs (mcp.md, SKILL.md) + changelog updated. Verified false-alarm claims (per-action auth, field-level write perms) already covered by `fields.writableBy` + `resolveActionPermission`; dynamic MCP instructions deferred (SDK construct-once fork).
- 2026-07-06 — modules — review follow-ups on the dependsOn work: (1) fixed 6 biome errors the dependsOn topological-sort code introduced (forEach-return + 4 non-null assertions in `orderModules` — the release gate `biome check src/` was red); (2) added the module-level `plugins?` phase (analog of app `plugins()` — runs after app plugins, in dependsOn order, before module bootstraps) so infra-registration is distinct from engine-init for ecosystem packages; (3) documented that `dependsOn` is COMPOSITION order, not lazy loading — all thunks resolve up front, dependsOn only orders the already-imported set. Tests: 3 new plugins-phase cases (ordering, dependsOn ordering, fail-fast). module.ts JSDoc + wiki/modules.md lifecycle + dependsOn sections updated.
- 2026-07-06 — modules — teardown-order bug fix (reviewer-caught, real): Fastify runs onClose hooks LIFO, so the separate module-teardown hook (registered before app onClose) actually fired AFTER app onClose — the reverse of the documented contract, and unsafe because app onClose closes shared infra (DB/Redis) that module teardown needs to flush outboxes/queues. Combined into ONE onClose hook running module.onClose (reverse composition order) THEN app onClose; registered last so it fires before the infra plugins' own onClose (LIFO). Verified Fastify 5.9 LIFO empirically. New test pins module.onClose-before-app.onClose (the gap: prior tests only covered module-to-module order). Enables module-owned DB/Redis: register in module.plugins, close in module.onClose. Fixed stale lifecycle summaries (registerResources fn header, factory/types.ts modules JSDoc) to include the plugins phase + correct onClose order.
- 2026-07-12 — index (Records section) — moved the two repo-root execution records into the wiki: `record-dependson-completion.md` (dependsOn completion report) + `record-ecosystem-extraction.md` (approval/involvement extraction record; the 2026-07-05 log entry's repo-root pointer updated). Records are point-in-time, not living pages.
- 2026-07-13 — factory, modules, plugins, permissions — 2.21 host-DX wave, derived from the be-prod audit (69k-LOC production host): `beforeBoot` pre-everything slot (kills the DB-before-engine dynamic-import hack) + module graph resolution hoisted before side-effecting phases; `defineModule({ errorMappers })` (host mappers keep priority); `loadResources({ filter })` path gate pre-import (feature-flag dir manifests become one callback); `ResourceSeams` + `mergeResourceConfig` + `AdapterLike` (cast-free programmatic assembly for module authors — kills the as-never tax; slot-aware merge: arrays concat, plain objects deep-merge, instances last-win); `permissionMatrix({ read, write, delete? })`; `schedulesPlugin` (interval jobs, LockAdapter leader safety, no Redis — cron-expression semantics stay in the BullMQ jobs tier). Deliberate NON-features: no resourcesFromEngine wrapper (domain tables are content, recipe documented in modules.md), no undeclared-event publish warning (dual-publish warn already covers the drift class). mongokit 3.21 pairs: adapter `schemaGenerator` defaults to `buildCrudSchemasFromModel` (28 silent-null-OpenAPI adapters in the audited host), `schemaGenerator: false` opts out.
- 2026-07-13 — schema-pipeline (new), index — pipeline armor after the customSchemas precedence bug: new page maps both halves (registry six-step + route-validation part-level precedence) with the knob decision table; `tests/core/schema-pipeline.test.ts` characterizes every registry step (was ZERO direct coverage — the gap the bug hid in). Spine follow-ups from the be-prod live smoke: arc-assets/arc-pricelist tenant `{field}`→`tenantField` behavior fix (custom tenant names silently ignored since 0.1.x), arc-assets wire contract now real customSchemas (JSON Schema, kernel-enum-sourced) with generator ON instead of the `schemaGenerator: false` opt-out.
