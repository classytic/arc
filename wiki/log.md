# Wiki Log

Append-only record of **wiki page** edits. Newest at bottom. Format: `YYYY-MM-DD — <page|all> — <wiki change>`.

This log tracks what's been documented in the wiki and why; release notes live in [`/changelog/v2.md`](../changelog/v2.md).

---

- 2026-04-21 — all — initial scaffold from CLAUDE.md + AGENTS.md (arc 2.10.3 baseline).
- 2026-04-24 — core — mixin-split surface documented (`BaseCrudController` + 4 mixins).
- 2026-04-24 — gotchas — added #19–24 covering 2.11 surface (mixin composition, `systemManaged` required-strip, `/types` cleanup, `defineResource` hygiene, schema-error warn, MCP file split).
- 2026-04-24 — core, gotchas — `createActionRouter` canonical preHandler order documented; gotcha #25 added.
- 2026-04-24 — testing — rewritten against the new `@classytic/arc/testing` surface (`createTestApp`, `TestAuthProvider`, `TestFixtures`, `expectArc`, `createHttpTestHarness`).
- 2026-04-24 — gotchas — added #33 for the websocket module split.
- 2026-04-24 — factory — documented `resources` accepting a sync/async factory function (between `bootstrap[]` and route wiring).
- 2026-04-24 — testing, removed — `createBetterAuthTestHelpers` / `setupBetterAuthOrg` / `safeParseBody` re-shipped; wiki sync.
- 2026-04-25 — adapters, gotchas — `fieldRules.nullable` documented; `default: null` widening on built-in mongoose fallback. Gotcha #26 (alongside `RouteSchemaOptions extends SchemaBuilderOptions`).
- 2026-04-25 — adapters — `RouteSchemaOptions extends SchemaBuilderOptions` (peerDep `>=0.2.0`); `ArcFieldRule extends FieldRule`. Documents the no-cast `schemaGenerator: buildCrudSchemasFromModel` wiring.
- 2026-04-25 — index, factory, events, plugins, removed — released wiki of historical changelog files (`changelog-v2.9/v2.10/v2.11.md` deleted; release notes belong under `/changelog/`). Cross-links updated to point at concept pages or the canonical changelog. Index "History" section collapsed to "API lifecycle" → `removed.md`.
- 2026-04-25 — factory — `loadResources({ context })` factory-export pattern documented; `silent` flag removal recorded with the arcLog fallback semantics (warn-by-default, suppressible via `ARC_SUPPRESS_WARNINGS=1` or per-call `logger` override).
- 2026-04-29 — events, adapters, peer-deps, identity — phase A–J relocation: events now sourced from `@classytic/primitives/events`; pagination / tenant / errors / schema-generator from `@classytic/repo-core/*`; `MongooseAdapter` and `DrizzleAdapter` schema-gen fallbacks deleted (kit `schemaGenerator` required). Peer-deps + identity bumped to 2.12.0 / primitives required.
- 2026-05-02 — adapters, peer-deps, index, factory, types, identity — arc 2.12 adapter split: every kit-specific adapter moved out of arc into its own kit. Adapter contract (`DataAdapter`, `RepositoryLike`, `AdapterRepositoryInput`, `OpenApiSchemas`, `SchemaMetadata`, `FieldMetadata`, `RelationMetadata`, `AdapterFactory`, `AdapterValidationResult`, `AdapterSchemaContext`, `asRepositoryLike`, `isRepository`) now lives in `@classytic/repo-core/adapter@>=0.3.1`. `createMongooseAdapter` ships from `@classytic/mongokit/adapter@>=3.13.0`; `createDrizzleAdapter` from `@classytic/sqlitekit/adapter@>=0.3.0`; `createPrismaAdapter` from `@classytic/prismakit/adapter@>=0.1.0`. `mergeFieldRuleConstraints` + `applyNullable` now in `@classytic/repo-core/schema`. Arc 2.12 is fully DB-agnostic — zero kit-specific adapters in `src/`. The `src/adapters/` directory was removed entirely; the internal `store-helpers.ts` moved to `src/utils/store-helpers.ts`. The `@classytic/arc/adapters` subpath export was removed from `package.json`. Mongokit, sqlitekit, mongoose, `@prisma/client` dropped from arc's peer-deps table — kits own their driver peers. Custom kits implementing `DataAdapter<TDoc>` plug in identically.
- 2026-05-03 — gotchas, identity — pruned wiki cruft. Deleted gotchas #19/#20/#22/#23/#24/#25/#26 (v2.11 history that's now just "how it works" — release notes belong in `/changelog/v2.md`, not gotchas). Trimmed #21 to the one load-bearing rule ("don't import values from `/types`"). Stripped version tags from #11/#12/#13/#15 and the dual-publish detector. Updated #9 to drop the rotting LOC count. Updated gotchas-file header to state "evergreen only — version-tagged behavior changes belong in changelog." Removed hardcoded `v2.12.0` from `identity.md` shape table (matches AGENTS.md "version numbers rot" stance).
- 2026-05-03 — skills/arc — pruned skill rot. Dropped frontmatter `version: 2.12.0` + `metadata.version` (versions rot — match the package). Dropped `progressive_disclosure.context_limit: 700` (file is 900+ lines; promise was false). Moved the 40-line subpath-imports enumeration out of `SKILL.md` into new `references/api-reference.md` (kept a 7-line common-imports block + a pointer in the entry). Trimmed the Better Auth overlay code block to a 3-line summary that points at `references/auth.md`. Convention-load-bearing content (RequestScope rules, field-write reject default, schemaGenerator-required, MCP fail-closed, multi-tenancy hierarchy, etc.) all preserved.
- 2026-05-03 — skills/arc-code-review — dropped pinned `arc_version` / `mongokit_version` / `sqlitekit_version` / `prismakit_version` / `repo_core_version` from frontmatter `metadata` (the skill versioned itself faster than the workspace; pin rotted). Skill body still cites kit subpaths by name, which is what consumers actually need.
- 2026-05-03 — index, skills/arc, docs/compliance — arc 2.13 enterprise-auth shipment. Three opt-in surfaces, no parallel infra, no forced peer-deps. (1) `@classytic/arc/scim` — full SCIM 2.0 plugin (filter parser, RFC 7644 PatchOp, bearer + verify auth, `/Users` + `/Groups` + discovery endpoints) auto-derived from existing arc resources via `mapping.attributes` (BA-aligned defaults). (2) `@classytic/arc/permissions` gains `requireDPoP` / `requireMandate` / `requireAgentScope` for AP2 / Stripe x402 / MCP authorization flows; `RequestScope.service` extended with optional `mandate` + `dpopJkt` fields (additive — no breaking change); new `Mandate` type + `getMandate` / `getDPoPJkt` accessors in `@classytic/arc/scope`. (3) `@classytic/arc/auth/audit` — `wireBetterAuthAudit({ events, transform, bufferSize })` returns BA-compatible `hooks` + `databaseHooks` config; routes auth events through existing `auditPlugin` so resource + auth audit rows live in one queryable store. New skills/arc/references/{scim,agent-auth,enterprise-auth}.md — surface-level recipes + `playground/enterprise-auth/` runnable smoke (11/11). New docs/compliance/{soc2,hipaa}.md mapping each TSC / Security Rule clause to the arc primitive that satisfies it. Arc still doesn't ship SAML / DPoP verification / mandate JWT parsing / device trust / session storage — see references/enterprise-auth.md "Out-of-box" table for why.
- 2026-05-03 — skills/arc/auth, skills/arc/SKILL.md — surfaced the BA plugin integration matrix that the kit tests actually cover (organization, twoFactor, admin, bearer, apiKey) but the docs glossed over. New "Better Auth plugin integration matrix" + "Multi-plugin recipe" + "Multi-role members" + "registerBetterAuthStubs full options" sections in [references/auth.md](../skills/arc/references/auth.md). Expanded API-Key section to flag the separate `@better-auth/api-key` npm package (NOT under `better-auth/plugins`) and added the `apikey` overlay recipe + the `configId` / `referenceId` field gotcha. Added a Bearer-plugin section for SPA/mobile transport. SKILL.md BA pointer expanded from 3 lines to a working multi-plugin recipe + multi-role + bearer + api-key flow summary so agents can wire a real BA stack from the entry doc without round-tripping the reference. Cross-link from the kit playgrounds preserved.
