# Wiki Log

Append-only record of **wiki page** edits. Newest at bottom. Format: `YYYY-MM-DD — <pages|all> — <one-line hook>`.

**Hard rule: ONE line per entry (~≤300 chars).** This file is loaded into agent/user context — it is an index, not an archive. Details belong in [`/changelog/v2.md`](../changelog/v2.md), the wiki page itself, or the pinning test; entries only say what changed and where to look. (Compacted 2026-07-15 from 34KB of paragraph-entries; full prose survives in git history.)

---

- 2026-04-21 — all — initial scaffold from CLAUDE.md + AGENTS.md (2.10.3 baseline).
- 2026-04-24 — core, gotchas, testing, factory — mixin-split surface; canonical preHandler order; gotchas #19–25/#33; testing page rewritten for `@classytic/arc/testing`; `resources` factory-function form.
- 2026-04-25 — adapters, gotchas, index, factory — `fieldRules.nullable`; `RouteSchemaOptions extends SchemaBuilderOptions`; historical changelog files deleted from wiki (release notes → /changelog); `loadResources({ context })`.
- 2026-04-29 — events, adapters, peer-deps, identity — 2.12 relocation: events → primitives; pagination/tenant/errors/schema-gen → repo-core.
- 2026-05-02 — adapters, peer-deps, index, factory, types, identity — 2.12 adapter split: kit adapters moved to their kits; contract in `@classytic/repo-core/adapter`; `src/adapters/` + `./adapters` subpath removed.
- 2026-05-03 — gotchas, identity, skills — version-tagged cruft pruned (gotchas #19–26 collapsed; skill version pins dropped; "evergreen only" rule added to gotchas header).
- 2026-05-03 — index, skills/arc, docs/compliance — 2.13 enterprise auth: `/scim` plugin, `requireDPoP`/`requireMandate`/`requireAgentScope`, `wireBetterAuthAudit`; SOC2/HIPAA mapping docs.
- 2026-05-03 — skills/arc/auth — BA plugin integration matrix (organization, twoFactor, admin, bearer, apiKey) + multi-plugin recipes; `@better-auth/api-key` package gotcha.
- 2026-05-07 — engine-backed-resources (new), index — 2.15 factory-export pattern promoted to a dedicated recipe page.
- 2026-05-18 — gotchas, mcp, peer-deps, removed, presets, permissions — 2.16 sync: per-resource `mcp: false`; floor bumps; `./org` removal recorded; softDelete kit-responsibilities table; "stamp-from-scope vs validate-from-scope" pattern.
- 2026-05-24 — mcp — 2.17 arc-ai DX: `createMcpAuthFromBetterAuthApiKey`; tool-name collision contract (`arc.mcp.tool_name_collision`).
- 2026-06-11 — architecture — internal-debt sweep: routerShared → `core/middlewares/`; BaseCrudController slimmed; distributed hardening. Zero API change.
- 2026-06-27 — encryption (new), index — ALE plugin at `/encryption` (JWE + field AES-256-GCM, KeyProvider/kid rotation, typed `extensions` hatch); all crypto in preSerialization, never onSend.
- 2026-07-05 — permissions, modules, presets, cli — 2.20 hardening: `readOnly()` denyAll on writes; public-by-omission diagnostic; typed module exports; init-scaffold live pins; `check:peer-skew` gate.
- 2026-07-05 — presets, exports — approval + involvement extracted to `@classytic/arc-approval`/`arc-involvement` pre-publish; rule: domain packs = `arc-*` ecosystem packages. See record-ecosystem-extraction.md.
- 2026-07-06 — mcp, core, modules — MCP list/get schema REST parity; per-route `rateLimit`; module `plugins?` phase; dependsOn = composition order; teardown-order bug fixed (one combined onClose: module teardown BEFORE app onClose — Fastify LIFO).
- 2026-07-12 — index — repo-root execution records moved into wiki (record-dependson-completion, record-ecosystem-extraction).
- 2026-07-13 — factory, modules, plugins, permissions — 2.21 host-DX wave (be-prod audit): `beforeBoot`, module `errorMappers`, `loadResources({ filter })`, ResourceSeams/`mergeResourceConfig`, `permissionMatrix`, `schedulesPlugin`; mongokit 3.21 schemaGenerator default.
- 2026-07-13 — schema-pipeline (new), index — pipeline page + full registry-step characterization tests, after the customSchemas precedence bug.
- 2026-07-14 — plugins, factory, core, testing, architecture, skills — 2.22 platform-accounting wave: `/usage` + `requireQuota`, `rateLimit.plan`, `history: true`, `bootModuleApp`; worker role (`createWorker`/`preset: 'worker'`/`mountRoutes: false`); BA adapter thunk; type mega-files split into submodule barrels; floors re-synced + `check:docs-drift` gate. Details: changelog 2.22.
- 2026-07-15 — factory, plugins, core, utils, changelog — 2.22 Fastify best-practices hardening (both waves): CORS wildcard+credentials boot throw; malformed bodies → 400 via shared `parseJsonBody`; `errorHandler.exposeInternalMessages`; request-id via `genReqId` (logs/header/requestContext agree); `arcLog` → pino bridge; OTel context actually active; all request props `decorateRequest`'d; timeout/trustProxy knobs; multipart caps; AUTH → route-level onRequest via `buildRouteHooks` (401 before body-parse/AJV; auth-before-parse.test.ts); `listResponse` oneOf → merged shape (byte-pinned); BA `rateLimit` knob. Details: changelog 2.22 hardening sections.
- 2026-07-15 — schema-pipeline, utils, core — zod v4 route/response support confirmed already wired, pinned by zod-route-schemas.test.ts (incl. response field-stripping); converter hardened: `UnconvertibleMode` (validation throws / docs warn), zod-v3 detection ("requires zod >=4"), `ensureSchemaConverter` readiness barrier.
- 2026-07-15 — factory — registerSecurity altitude split → `factory/security/{pluginLoader,rateLimit,cors}.ts`; `resolveCorsOptions` pure (all five CORS policies incl. exposed-header + maxAge); allowedHeaders host-array mutation fixed (copy-before-merge, no-mutation test).
- 2026-07-15 — log, factory, plugins, core, record-dependson — wiki hygiene sweep (user granted standing edit/drop authority): log.md compacted 34KB→5.8KB with a one-line-per-entry hard rule in its header; today's hardening sections in factory/plugins/core tightened to contract-plus-pointer form; record-dependson-completion compressed to shipped-list + verification table (living docs = modules.md). modules.md kept as-is (dense contract, not narrative). Stale-reference grep clean.
- 2026-07-15 — designs, factory, changelog — designs/worker-role.md DELETED (feature shipped in 2.22/2.23; spec was a build-time artifact). Unique "Deployment & compatibility" answers salvaged into factory.md § createWorker (two-entrypoint topology, nobody-must-run-one, import home); all six pointers (wiki, changelog, 4 src comments) redirected to wiki/factory.md.
- 2026-07-15 — permissions, designs/record-sharing — `requireGrant` shipped (the verdict's "when first host asks" trigger fired): gate + GRANT_MODES lattice + modeSatisfies in src/permissions/grants.ts; storage stays host-owned; grounded against a Puter source audit (typed modes vs string perms, query-level filters vs N+1 ACL walks, fail-closed, revocable link rows). Pinned by tests/permissions/require-grant.test.ts (12) incl. filters-reach-repo-query e2e; verdict doc → SHIPPED; contract pin gains requireGrant.
- 2026-07-15 — designs/record-sharing, permissions — record sharing FULLY shipped: `@classytic/arc-shares` 0.1.0 built in arc-ecosystem (ShareStore equality-only port + repository adapter, createShares service, revocable HMAC link rows, prebuilt permission sets on requireGrant). Key design catch: ownership folds INTO the resolver (owner fallback = manage+owner-filter) — anyOf(requireOwnership, requireGrant) short-circuits because ownership always grants via filters; read checks _isPublic-marked when links enabled. 18-test suite incl. full arc e2e (owner/grantee/anonymous-link/revocation); workspace verify green (102 tests, 6 builds, peer-skew OK via file:../arc link until 2.22 publishes).
- 2026-07-15 — plugins, changelog — `realtimePlugin` shipped (Supabase-gap #1): permission-gated per-resource SSE change feed composing existing primitives (CRUD event bridge + list-permission gate + in-process row-filter matching + tenant guard + field masking); fail-closed 501 on unmatchable operator filters; SSE transport extracted to utils/sseStream.ts (sse plugin refactored onto it, 18 sse tests unchanged-green); RegistryEntry gains live `fields` map. arcPlugins.realtime wiring + plugins barrel exports. 8-scenario e2e via real HTTP (anonymous public feed, 401→token, per-event owner filtering, masking, tenant drop, unfilterable rejection, matcher-supplied grants, 404/allowlist). 2171 tests green; tsc/biome/knip/build/smoke clean.
- 2026-07-15 — plugins (realtime polish) — self-review after "is the code dirty?": applied the same altitude standard as registerSecurity — pure policy extracted from the 135-line handler (`resolveRowMatcher` enforcement decision, `resolveDelivery` per-event authorization as ONE pure function, `buildChangeFrame` envelope; exported-for-tests convention) with 10 new no-HTTP unit tests; SSE spec gaps closed (`id:` per frame → client lastEventId dedup, `retry:` hint at connect — WHATWG fields Mercure/graphql-sse ship); envelope divergence from ssePlugin documented as deliberate (document-change-shaped vs raw event mirror); fixed a null-sentinel ambiguity by making resolveDelivery a discriminated result. 35 realtime+sse tests green.
- 2026-07-15 — plugins (realtime multiplex) — Mercure-style multiplexing shipped after the "industry needs concurrent feeds" challenge: `GET /realtime?resources=a,b,c` = N feeds over ONE connection (cap 20), per-resource authorization with ISOLATED filter snapshots (save/reset/restore _policyFilters around each evaluateAndApplyPermission — accumulation would leak resource A's filters onto B; pinned by the mixed public+owner-filtered multiplex test), any-denial rejects whole subscription. Handler refactored into shared per-resource pipeline (runAuth/authorizeResource/streamSubscriptions) feeding both routes. Encryption verdict recorded: TLS + per-subscriber field masking IS the industry posture (Supabase/Pusher ship no payload E2EE; Signal-style E2EE is user-to-user messaging, not server-authoritative feeds) — no gap. 38 realtime+sse tests green.
- 2026-07-15 — plugins (realtime perf + positioning) — shared-frame WeakMap: unmasked subscribers of the same event share ONE JSON.stringify (N-subscriber fan-out was N stringifies; masked subscribers still pay their own — masking is per-role). plugins.md gains the 'choosing the push channel' table (realtime = per-subscriber authorized resource feed; websocket = bidirectional/rooms/geo, CRUD bridge is org-coarse — do NOT expose row-permissioned resources through it; sse = event firehose). Scale ceilings documented in the answer record: per-instance SSE fan-out is a Node process (~thousands of subscribers), horizontal via Redis Streams transport; massive fan-out (100k+ viewers) belongs in a dedicated edge layer fed by arc events.
- 2026-07-15 — docs (realtime.mdx new), skills/arc/production — host-facing realtime guide shipped: plugin usage + push-surface decision table + THREE scaling tiers (single instance → Redis Streams horizontal → edge fan-out for viral/scoreboard loads with Ably/Centrifugo/Cloudflare/DIY-gateway recipes and the four Tier-3 design rules: publish the public shape, capability tokens for private channels, meta.id dedup, no dual-serving). Answers 'what tool for viral feeds' — arc needs no new runtime (egress = one events.subscribe); the gap was documentation. _meta.js + skill production ref synced.
- 2026-07-16 — (kits) — `matchesFilter` seam implemented for BOTH mongokit + sqlitekit, unblocking grant-scoped realtime (operator-shaped `_policyFilters` were hitting the fail-closed 501). PRINCIPLED divergence, not duplication: mongokit ships a standalone Mongo matcher (`src/filter/match-in-memory.ts`) because Mongo needs ObjectId/Date string coercion that repo-core's strict-`===` `matchFilter` won't do; sqlitekit ships a THIN converter (`policyRecordToFilter`: Mongo-record `$or`/`$in`/`$`-ops → repo-core Filter IR) delegating to the shared `matchFilter` — same IR it compiles to SQL, so in-memory and DB enforcement agree. Both fail-loud on unsupported ops. Future consolidation (rule-of-three): when pgkit/prismakit need it (primitive ids, like sqlite), hoist `policyRecordToFilter` to repo-core; mongokit stays standalone for the ObjectId concern. Blocked on release chain (kits have INSTALLED repo-core copies at 0.12/0.8, not local links — can't hoist without a repo-core publish + reinstall). mongokit 2633 tests green (CHANGELOG 3.23.0-unpublished), sqlitekit 650 green ([Unreleased]); removed a corrupt untracked orphan (mongokit src/adapter/match-filter.ts) that was breaking tsc.
- 2026-07-16 — (repo-core + kits + arc) — matchesFilter HOISTED to repo-core, whole family aligned on ONE repo-core line (0.14.0). Killed the DRY + version skew from the previous turn: repo-core 0.14.0 ships canonical `matchesRecordFilter`/`policyRecordToFilter` (`@classytic/repo-core/filter`) — the single `DataAdapter.matchesFilter` impl every kit delegates to (converts arc's Mongo-record `_policyFilters` → portable Filter IR → shared `matchFilter`, the SAME IR kits compile to SQL/Mongo). repo-core's `matchFilter` `equals` made id-coercion + array-contains aware (additive superset), so ONE matcher serves Mongo ObjectId `_id`s AND SQL primitive ids — mongokit no longer needs its own coercing matcher. Deleted both per-kit matchers (mongokit src/filter/match-in-memory.ts, sqlitekit src/filter/match-in-memory.ts) + their exports/tests; both adapters now `matchesFilter = matchesRecordFilter`. Floors bumped >=0.14.0 across mongokit (was 0.13, closed 0.12/0.13 skew), sqlitekit (was 0.8), arc (was 0.7; CLAUDE.md + peer-deps.md synced, check:docs-drift + check:peer-skew green). Verified against staged 0.14.0: repo-core 555, mongokit 2607, sqlitekit 632, arc 1555 — all green; every package tsc/lint/build clean. RELEASE CHAIN (user's duty): publish repo-core 0.14.0 → then mongokit/sqlitekit/arc reinstall resolves the real package (staged copies are dev-links, identical to publish artifact via npm pack).
- 2026-07-16 — (repo-core filter engine) — matchesFilter contract made COMPLETE + hardened after "all abilities or restricted?" probe. Found the shared repo-core matcher was RESTRICTED vs mongokit's original: subdocument-array dot-path fan-out SILENTLY returned false (worst kind — a realtime subscriber could miss events), $regex fail-loud, leaf-array range/regex didn't unwrap. Closed ALL in repo-core's `matchFilter` engine (one place, every kit + arc benefit): array-aware via a single `someValue` unwrap helper (dot-paths fan out over subdoc arrays; scalar conditions on leaf arrays match any element for eq/in/range/regex), `$regex`(+`$options`/RegExp literal) added to policyRecordToFilter, id-coercion kept, Date⇄ISO-string range in `compare` (+ number/string type-boundary guard so ranges fail-closed not spuriously), and prototype-pollution-safe reads via `Object.hasOwn` (crafted `__proto__`/`constructor` paths can't traverse the chain). `$exists` documented as present-and-non-null (cross-dialect: SQL has no key-absence). Comprehensive edge/extreme/perf suite added (32 matcher cases incl. NaN/Infinity, boolean/zero, empty logical arrays, prototype pollution, fail-loud; 100k-doc linear perf smoke + regex-cache reuse). repo-core 555→576 green; re-staged 0.14.0 into all 3 consumers, re-verified: mongokit adapter 25, sqlitekit adapter 2, arc realtime 20 — green. Full ability parity, nothing lost in consolidation.
- 2026-07-16 — (repo-core filter engine + arc realtime) — MongoDB-PARITY hardening after web research (MongoDB manual + sift.js + mingo). Validated the shared matcher against the famous query gotchas; the two most dangerous were ALREADY correct (missing-field ≡ null for {field:null} and $ne/$nin — the top silent-authorization-divergence). FIXED the real divergences: $in/$nin with a null member now matches missing ({tenant:{$nin:[null,'public']}} correctly requires present+non-null); $in accepts RegExp-literal members; NaN equals NaN for $eq (Object.is not ===); numeric dot-path segments resolve as positional array indices (items.0.sku). Kept + DOCUMENTED deliberate divergences: $exists=present-and-non-null (matches IR exists op + SQL IS NOT NULL + sift; Mongo/mingo differ on present-null — would need a new key-presence IR op through every kit compiler); Date⇄ISO-string range leniency; array-literal operand element-matched. ROBUSTNESS: arc realtime now PROBES the filter at CONNECT (matchesFilter({},filters) in try/catch) so a custom permission returning an unsupported operator ($elemMatch/$where) rejects cleanly as 501 instead of THROWING per-event and crashing the subscriber's stream. Tests: repo-core filter suite 41 cases (11 researched gotchas locked) + 100k perf smoke; repo-core 586 green; re-staged into all 3; mongokit adapter 25, sqlitekit adapter 2, arc realtime 21 green. Everything tsc/lint clean.
- 2026-07-17 — (repo-core filter + arc realtime + mongokit) — "are we doing it right?" answered with TWO web-research passes (realtime-authz architecture: Supabase/Hasura/Firestore/ElectricSQL/Meteor/RethinkDB; and query-matcher-as-authz security: ReDoS/prototype-pollution/NoSQL-injection/semantic-drift). VERDICT: architecture is sound + industry-standard (= Supabase Broadcast-from-DB / ElectricSQL shape model). Fixed the two real gaps it surfaced: (1) LEAVE-detection — arc realtime now emits a synthetic `<resource>.left` frame when an UPDATE moves a record OUT of a subscriber's filter (was: silent drop → permanently stale row; the "row left the result set" signal every mature system sends). resolveDelivery → 3-way deliver/leave/drop, threaded with operation; ENTER needs no signal (matching update carries the record). (2) STALENESS bound — feed force-closes at token exp (`connectionDeadlineMs`, min(exp, maxConnectionMs), setTimeout-ceiling-clamped) → reconnect re-authorizes; bounds the "revoked caller keeps old view" leak (Supabase JWT-TTL posture). Plus: connect-time filter probe already rejects unsupported-operator throws cleanly (501, not per-event crash). Security hardening in the shared matcher: prototype-key denylist on path segments (fail-closed, string-normalized — object-path CVE lesson; closes JSON.parse own-__proto__ + inherited-member-match), $regex 64KiB input cap (ReDoS pattern×input, fan-out amplification), reaffirmed no cross-type coercion. Anti-DRIFT gate: new mongokit differential test runs 13 filter shapes × 5 edge-docs through BOTH matchesRecordFilter AND real MongoDB, asserts identical — found + pinned the SOLE divergence ($exists on present-null: Mongo key-presence vs matcher non-null, documented). Verified: repo-core 586, mongokit 2609, sqlitekit 631 (1 flaky lock-timing unrelated), arc 1713 + realtime 29 green; knip clean. Docs: realtime.mdx client contract for .left + staleness/reconnect + self-contained-filters; changelogs across repo-core/arc.
- 2026-07-17 — (arc audit vs external review) — External (OpenAI) architecture review verified claim-by-claim with three parallel audit passes; fixed what was real. (1) SECURITY: MCP tools bypassed field-level READ masking — `fields.hidden()`/`visibleTo()` masked HTTP + realtime but leaked verbatim through get/list/action/aggregation MCP tools. New `applyMcpReadMasking` (mcp/tool-helpers.ts) at the serialization boundary of all four tool families, HTTP-identical semantics (elevated bypass, role union, non-downgrade scope override, paginated shapes); write side was never affected (BodySanitizer is controller-level). 14-test pin (mcp-field-masking.test.ts). (2) GATE: `check:boundaries` (zero-dep script, prepublishOnly) — layered module map, fails upward RUNTIME imports + new cycles; type-only/dynamic imports exempt (erased/deferred — can't init-cycle); 3 grandfathered edges tracked for burn-down. (3) DOCS: architecture.md de-rotted (stale adapters/ row + line-count table dropped); NEW extension-decision.md (which seam for which need) + delivery-guarantees.md (one matrix across 9 async channels); index updated. Lint gate fixed (4 stale errors). Verified: tsc, biome, MCP suite 408+14 green. Review claims that were ALREADY FIXED/wrong: config namespacing (arcPlugins/stores), introspection (describe/introspect/docs + /_resources), docs-drift checks, action fail-closed parity. Still-open (design-scale, not this pass): JSON Schema export for CreateAppOptions/ResourceConfig, operation-effect metadata (dataSensitivity/confirmation), transport-independent operation executor (invokeController is the seed), subpath stability labels.
- 2026-07-17 — (arc MCP execution parity + stability labels) — Review follow-up items #1 (transport-independent executor) and #4 (subpath stability) implemented after verifying the ACTUAL gap: hooks live on `metadata.arc.hooks` (stamped by the HTTP arc decorator) and the arcCorePlugin after-hook is what publishes CRUD events — MCP's synthetic context carried neither, so EVERY MCP mutation skipped hooks AND event publishing (realtime/webhooks/subscribers blind to agent writes; much bigger than the review's "hooks+idempotency" framing). Fix: `McpExecutionWiring` threaded `resourceToTools({wiring})` → all 4 tool families → `buildRequestContext(extras)` which now assembles metadata.arc + first-class scope projection + server accessor (shape-identical to HTTP's createRequestContext); mcpPlugin auto-wires via lazy fastify getters (order-agnostic). Idempotency: `_idempotencyKey` on mutating tools (advertised ONLY when store wired), executor check→lock→execute→record→replay against the SAME HTTP store (`fastify.idempotency.store` now exposed), fingerprint = tool+sorted-input-hash+caller; errors never cached. invokeController = the de-facto executeResourceOperation (permission→context→idempotency→dispatch→masking→error). Stability: `arc.subpathStability` in package.json labels all 51 subpaths (experimental: sync/usage/event-gateway); contract test enforces exact coverage both directions. 13-test parity pin (mcp-execution-parity.test.ts, incl. no-wiring control + hooks×masking compose). NOT done deliberately: hooks for aggregations (HTTP doesn't run them either — parity, not features), JSON Schema config export (user agreed: over-engineering).
- 2026-07-17 — (delivery-guarantees matrix completed) — Self-audit of the P2 matrix against the review's 8 requested dimensions found ONE real gap: the backpressure column was missing (other 7 covered). Added it with code-verified per-channel behavior, not guesses: memory=inline sequential await (no queue); pub/sub=none (unbounded client buffer); Streams=producer-side XADD MAXLEN ~10k trim (SILENT loss past the window — new composition rule: size to downtime×rate or front with outbox); outbox=pull-based bounded relayBatch (durable DB backlog); SSE/realtime=fail-fast destroy on full socket (sseStream.ts:100); jobs=BullMQ concurrency + arc maxConcurrent semaphore (Redis-durable backlog); schedules=self-limiting timeout chain (slow handler stretches interval, never stacks); webhooks=bounded per-event concurrency (5) + timeout abort. index.md hook + changelog synced.
- 2026-07-17 — (external-review wave 2 — all six claims verified REAL, zero false positives) — (1) production preset trustProxy:true: boot WARN only when INHERITED (options.trustProxy undefined + preset production); explicit values silence. (2) edge.ts rebuilt as honest "buffered fetch bridge": arrayBuffer() request bodies (text() corrupted binary), rawPayload responses, ONE shared readiness promise (cold-start race), maxRequestBytes 1MiB default → 413 arc.payload_too_large pre-buffer; docs state SSE/WS/streaming/backpressure unsupported by construction. (3) webhooks: v1 signed contract `v1.<tsMs>.<deliveryId>.<body>` → x-arc-webhook-{signature,timestamp,delivery} headers + verifyWebhook (tolerance 5min default, typed failure reasons, returns AUTHENTICATED delivery id = event meta.id for redelivery dedup); legacy body-only headers kept (v3 removal); validateUrl SSRF seam + opt-in defaultWebhookUrlPolicy (https + loopback/private/link-local/metadata block; NOT default — internal webhooks legit; no DNS resolution — rebinding belongs at egress); ⚠ list() secret-redacted with getSecret(id) privileged path; concurrency>=1 boot validation (0 = infinite splice loop). (4) resolveOrgFromHeader throws UnauthorizedError/OrgAccessDeniedError (canonical ErrorContract; legacy {success,error,message} envelope killed) + preserves adapter-resolved scope.userRoles on authenticated→member upgrade (review's "getUserRoles reads role+roles" was imprecise — getUserRoles only reads .role; the real fix is scope preservation). (5) ApiResponse @deprecated → IControllerResponse/ErrorContract. (6) ArcHeaders type + getHeader() accessor in utils; IRequestContext.headers JSDoc honest about runtime string[] behind the narrow type; declared-type widening deferred to v3 (minor-version break otherwise). Multi-domain composition guidance appended to production.md (host-resolved domain → tenant CANDIDATE, membership authorizes — never raw Host as authz). Tests: v1-contract (14), edge-buffered-adapter (6), resolveOrgFromHeader rewritten (+2 role-preservation). 
- 2026-07-18 — (external-review wave 3 — audit of wave 2's own fixes; all 4 points real, fixed) — (1H) webhook URL policy had TWO bypasses: persisted store rows skipped register()-time validation (plugin init now re-validates every stored sub; violators EXCLUDED from dispatch + loud log, not boot-fail — poisoned row ≠ outage), and fetch followed redirects (allowed public URL could 302 → metadata/private; policy-active delivery now redirect:"manual", 3xx = failed delivery). defaultWebhookUrlPolicy gained IPv6: ::1/::/fc00::7/fe80::10/0.0.0.0 + IPv4-mapped in BOTH spellings — KEY FINDING: WHATWG URL canonicalizes ::ffff:169.254.169.254 → ::ffff:a9fe:a9fe hex groups, so a dotted-quad-only matcher NEVER fires post-parse; decodeMappedV4 handles both. (2M) trustProxy: informed-tradeoff stance kept for v2 (flip would break every proxied prod host silently); fail-closed flip formally scheduled in v3.md §11 + warning text says so. (3M) resolveOrgFromHeader rejects repeated org header (400 ValidationError) instead of casting the runtime array — first consumer of getHeader()'s duplicate-rejection posture. (4L) edge maxRequestBytes docs precise: pre-buffer only with declared Content-Length; chunked = post-buffer check; platform limits are the real pre-allocation guard. v3.md gained a "breaking hygiene" section (trustProxy flip, legacy webhook header removal, ApiResponse removal, ArcHeaders widening). Tests: +6 (persisted-exclusion, redirect manual/default/3xx-fail, IPv6 block set + public-v6 allowed, duplicate-header 400). All gates + 182 affected tests green.
- 2026-07-18 — (external-review wave 4 — arc init dependency weight) — Concrete claim CONFIRMED: mongodb-memory-server sat in devCommon, installing a full MongoDB binary download for EVERY scaffold including --adapter custom. Moved to new devMongokit group assigned only with adapterMongokit (the generated test harness's import was already mongokit-conditional in templates/auth.ts, so the move is safe — verified before editing). +2 dep-plan tests (mongokit gets it; custom excludes it + mongoose + @classytic/mongokit while keeping common tooling); fixed a stale pre-existing fixture in dependency-plan-latest.test.ts (repo-core 0.13.0 latest fell below the 0.14 peer floor bumped this wave — peer-overlay-wins-last correctly lifted it; fixture now 0.99.0). The review's broader "complete platform install" observation is DESIGN INTENT (arc init scaffolds a production app; mongokit+BA are the defaults) — not changed. SURFACED, not fixed (deeper known scaffold gap, matches memory note arc-init-scaffold-gaps): the Better-Auth template hard-codes mongodbAdapter(mongoose.connection...) (templates/auth.ts:151-169), so auth=better-auth + adapter=custom generates code referencing mongoose that isn't installed — the mongodb dep in authBetterAuth exists for that template; fixing means a DB-agnostic BA template (kysely/drizzle adapter seam), a separate scaffold-redesign decision.
- 2026-07-21 — delivery-guarantees — webhook opt-in retry row + new Distributed deployment checklist (guard-visible vs host-audited subsystems); wave-5 audit. Fix details in changelog 2.24.0.
- 2026-07-21 - delivery-guarantees, peer-deps-adjacent - wave-6 repo-core convergence: CacheStore extends CacheAdapter (increment signature fix), LockAdapter/UsageStore/scheduleBackground re-exports, withRetry DLQ rethrow, job/health AbortSignals, schedule lease renewal. Details in changelog 2.24.0.
- 2026-07-21 - (no wiki pages) - wave-7 failure-safety closure: eventPlugin durable deadLetter slot, TTL-on-create increment, serialized lease renewal (schedules+migrations), settle-based job bulkhead + cancelGraceMs, contained compensation hooks. v3.md restructured: breaking changes approved for minors (2.25 plan). Details in changelog 2.24.0.
- 2026-07-21 - factory, delivery-guarantees-adjacent - breaking-hygiene batch shipped in 2.24: production trustProxy=false, v1-only webhook headers (signPayload removed), ApiResponse removed, IRequestContext.headers=ArcHeaders, populate/lookup deny-by-default. Migration notes in changelog 2.24.0.
- 2026-07-21 - (no wiki pages) - wave-8: atomic Redis TTL-on-create (expireIfAbsent Lua seam), src/lock renewable lease (shared schedules+migrations, loss ENFORCED in migrations), jobs-execution primitive extracted, module/ split, tests/_support helpers, 12 regression files merged, tests/integration->adapters, version-named tests renamed, test lanes, .env live-Redis contract suite. Changelog 2.24.0.
- 2026-07-22 - (no wiki pages) - wave-9 adoption readiness: enforced typecheck:types lane (caught 4 type drifts incl. RouteDefinition.handler variance fix), api-surface.json gate (52 subpaths/1089 names), GOVERNANCE+SECURITY+CONTRIBUTING+templates, publint+attw in prepublishOnly, tests/README rewritten. Changelog 2.24.0.
- 2026-07-22 - (no wiki pages) - wave-9 completion: jobs/ + outbox/ mechanical splits (byte-identical bodies, jobs subpath -> jobs/index, outbox.ts compat barrel), CI matrix Node 22+24 + peer-minimums floor job, api-surface regenerated. Changelog 2.24.0.
- 2026-07-22 - (no wiki pages) - wave-10: api-surface gate diffs runtime/types independently + declaration hashes; all 12 expectTypeOf files compiled in the type lane (fixed vitest-hidden drift incl. ArcXResult-this circularity doc bug); 24 noisy default-logger tests silenced. Changelog 2.24.0.
- 2026-07-22 - (no wiki pages) - wave-11: stable-subpath declaration-hash changes now FAIL check:api-surface; new check:type-lane self-enforces expectTypeOf coverage in the compiled lane; repository URL git+https. Changelog 2.24.0.
- 2026-07-22 - cache, delivery-guarantees - wave-12 performance audit: under-pressure real default thresholds, cache single-flight + jitter, multipart maxTotalBytes, two-phase outbox claim (~2 RTT), Streams processingConcurrency, WS head-pointer drain, bucketed health metrics; bench lane + arc doctor + streaming multipart staged in v3.md. Changelog 2.24.0.
- 2026-07-22 - events, removed, delivery-guarantees-adjacent - outbox contract ownership settled: @classytic/primitives/outbox (>=0.13) owns OutboxStore + option types + error classes; arc 2.24 deleted its duplicate (clean break, no re-export) and keeps runtime only (EventOutbox, MemoryOutboxStore, repositoryAsOutboxStore, exponentialBackoff). Fixes cross-package instanceof. Peer primitives >=0.13.0. Changelog 2.24.0.
- 2026-07-28 - modules - documented the 2.24 self-describing arms in the contract block + a new section, and the 2.29 opt-in `boundary` on `eventHandlers` (off by default so a throw still reaches the transport and DLQs; on for fire-and-forget). Authoring example now uses the `eventHandlers` arm instead of `afterResources` — the arm unsubscribes before module `onClose`, which a hand-rolled subscribe cannot. Changelog 2.29.0.
- 2026-07-28 - hooks, modules - HookContext.scope: global hooks now carry the same RequestScope projection as ResourceHookContext/IRequestContext (one helper, no drift); documented "read ctx.scope, never ctx.user". Changelog 2.29.0.
- 2026-07-28 - permissions, hooks, modules - 2.30 authorization wave: decision-only contract (allow/deny, PDP evaluatePermissionDecision + PEP applyAuthorizationDecision; PermissionResult/normalizePermissionResult REMOVED), conjoinPolicyFilters AND-composition, repository-boundary filter normalization for non-Mongo kits, fail-closed ownedByUser, explainAccess/describePermission, runAuthorizationConformance, strictPermissions. Changelog 2.30.0.
